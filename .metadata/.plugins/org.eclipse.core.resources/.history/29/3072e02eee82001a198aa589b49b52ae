package playerActions;

import game.Direction;
import game.Entity;

public class ActionMove extends Action{

	Direction m_dir;
	
	public ActionMove(Direction dir, int percentage) {
		super(percentage);
		m_dir = dir;
	}
	
	@Override
	public boolean apply(Entity e) {
		return e.move(m_dir);
	}
	
	
}
