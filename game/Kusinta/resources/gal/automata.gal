Player_donjon(Init){	
  * (Init)
  | Cell(H,A) ? Power() : (Init)       
  | ! GotPower() ? Explode () : ()     
  | Key(z) ? Jump: (Jumping)
  | Key(q) ? Move(W) : (Init)
  | Key(d) ? Move(E) : (Init)
  | Key(s) ? Wizz()  : (Init)          
  | Key(SPACE) ? Egg() : (Init)
  | Key(v) ? Pop() : (Init)    
  
  * (Jumping)
  | Cell(H,A) ? Power() : (Init)       
  | ! GotPower() ? Explode () : ()  
  | Key(q) ? Move(W) : (Init)
  | Key(d) ? Move(E) : (Init)
  | Key(s) ? Wizz()  : (Init)          
  | Key(SPACE) ? Egg() : (Init)
  | Key(v) ? Pop() : (Init)  
  | Key(z) ? Jump: (Jumping)
}

Block (Inactif) {                
  *(Inactif) 
  | True ? : (Inactif)
}

Fleche (Init) {
  *(Init)
  | Cell(H,V) ? Move : (Init)
  | Cell(H,_) ? Explode() : ()
}

Decor (Inactif) {                
  *(Inactif) 
  | True ? : (Inactif)
}